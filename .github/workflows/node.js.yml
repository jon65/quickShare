name: CICD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2 #needed to import source code to runner env

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: install dependencies
        run: npm install 

      - name: Set up Docker Compose
        run: |
          sudo apt-get -yqq install docker-compose
          docker-compose version
          
      - name: Build and Test
        run: |
          docker-compose up --build -d
          docker-compose down
